program pr_4;
var
n, k, i, x: real;
begin
writeln('¬ведите 2 числа');
readln(n);
readln(k);
i:= n / k;
if i > 0
then x:= x + 1;
n:= n -1;
if n = 0
then writeln(i);
i:= n / k;
if i > 0
then x:= x + 1;
n:= n -1;
if n = 0
then writeln(i);
i:= n / k;
if i > 0
then x:= x + 1;
n:= n -1;
if n = 0
then writeln(i);
i:= n / k;
if i > 0
then x:= x + 1;
n:= n -1;
if n = 0
then writeln(i);
i:= n / k;
if i > 0
then x:= x + 1;
n:= n -1;
if n = 0
then writeln(i);
i:= n / k;
if i > 0
then x:= x + 1;
n:= n -1;
if n = 0
then writeln(i);
i:= n / k;
if i > 0
then x:= x + 1;
n:= n -1;
if n = 0
then writeln(i);
i:= n / k;
if i > 0
then x:= x + 1;
n:= n -1;
if n = 0
then writeln(i);
i:= n / k;
if i > 0
then x:= x + 1;
n:= n -1;
if n = 0
then writeln(i);
i:= n / k;
if i > 0
then x:= x + 1;
n:= n -1;
if n = 0
then writeln(i);
i:= n / k;
if i > 0
then x:= x + 1;
writeln(x);
end.